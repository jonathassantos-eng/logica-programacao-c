#include <stdio.h>

int main (){
    int a = -10, b = 5; // Declaração e inicialização de duas variáveis inteiras, 'a' e 'b', com os valores 10 e 5, respectivamente.

    if(a > 0 && b > 0)
    { 
        printf("Pelo menos um dos números é positivo. \n");
    } else {
        printf("Os dois números são negativos. \n");
    }
    
}


